---
- name: Gather machine facts for Orthos2
  hosts: all
  gather_facts: yes

  roles:
    - add_custom_facts

  tasks:
    - name: Ensure all facts are gathered
      setup:
        gather_subset: all
