#!/bin/bash

# Django wrapper script for orthos server management
DJANGO_SETTINGS_MODULE=orthos2.settings
export DJANGO_SETTINGS_MODULE

if [ "$USER" == "orthos" ]; then
  django-admin $@
else
  sudo -E -u orthos django-admin $@
fi
